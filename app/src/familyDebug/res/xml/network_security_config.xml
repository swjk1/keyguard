<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only.

  Android blocks cleartext HTTP by default, which would make a local dev server fail with a
  confusing connection error rather than an obvious one. This permits cleartext to loopback
  addresses and nothing else, so the exception cannot be used to reach a real host.

  10.0.2.2 is the emulator's alias for the host machine. For a physical device use
  `adb reverse tcp:3000 tcp:3000`, which makes localhost on the device reach the host.

  Release builds do not include this file and remain HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
