<?xml version="1.0" encoding="utf-8"?>
<!--
  The child app's shell: a title bar, a section, and the navigation between sections.

  This file used to be the whole child app - 733 lines holding disclosure, permissions, keyboard
  setup, seven sliders, a live keyboard preview, an AI toggle, the pairing form and the
  statistics, in one scroll. That is why it read as a demo: everything was equally prominent,
  which is another way of saying nothing was, and the question a user actually opens the app to
  ask ("am I protected?") was six screens of scrolling from an answer.

  Now it holds nothing but the frame. Each section is a fragment that owns its own state and its
  own layout, and can be read on its own.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface"
        app:elevation="0dp"
        app:liftOnScroll="false">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/surface"
            app:titleCentered="false"
            app:titleTextColor="@color/on_surface" />
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/sectionContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        style="@style/Widget.Keyguard.BottomNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:menu="@menu/nav_child" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>
